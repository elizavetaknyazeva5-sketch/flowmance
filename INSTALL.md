# Инструкция по установке и запуску проекта

## 1. Установка зависимостей

Убедитесь, что виртуальное окружение активировано (вы должны видеть `(flow)` в начале строки).

### Установка всех зависимостей из requirements.txt:
```bash
pip install -r flow/requirements.txt
```

### Или установка основных библиотек вручную:
```bash
pip install fastapi uvicorn sqlalchemy psycopg2-binary pydantic-settings fastapi-users fastapi-users-db-sqlalchemy
```

**⚠️ ВАЖНО:** 
- НЕ устанавливайте `sqlalcheny.orm` (это опечатка и неправильное название)
- Устанавливайте просто `sqlalchemy` - он уже включает всё необходимое

## 2. Запуск проекта

### ⚡ Быстрый запуск (рекомендуется):

**macOS/Linux:**
```bash
chmod +x run.sh
./run.sh
```

**Windows:**
```bash
run.bat
```

Скрипты автоматически активируют виртуальное окружение и запустят приложение.

### 📝 Ручной запуск:

**⚠️ ВАЖНО:** Команду нужно запускать из **корневой директории проекта** (`/Users/ivanfrolovich/Desktop/flowmance`), а не из папки `app/`!

1. **Убедитесь, что вы в корне проекта:**
```bash
cd /Users/ivanfrolovich/Desktop/flowmance
# или просто
cd ~/Desktop/flowmance
```

2. **Активируйте виртуальное окружение:**
```bash
# macOS/Linux
source flow/bin/activate

# Windows
flow\Scripts\activate
```

3. **Запустите приложение:**
```bash
uvicorn app.main:app --reload
```

**НЕ используйте:** `uvicorn app.core.main:app` ❌ (неправильный путь)

**Используйте:** `uvicorn app.main:app` ✅ (правильный путь)

### Параметры запуска:
- `--reload` - автоматическая перезагрузка при изменении кода (для разработки)
- `--host 0.0.0.0` - доступ с других устройств (опционально)
- `--port 8001` - указать другой порт (по умолчанию 8000)

### Полная команда с параметрами:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

## 3. Проверка работы

После запуска откройте в браузере:
- http://localhost:8000 - главная страница
- http://localhost:8000/docs - автоматическая документация API (Swagger)
- http://localhost:8000/redoc - альтернативная документация (ReDoc)

## 4. Если uvicorn не найден

Если появляется ошибка `CommandNotFoundException`, значит uvicorn не установлен:

```bash
pip install uvicorn
```

Или переустановите все зависимости:
```bash
pip install -r flow/requirements.txt
```

## 5. Ошибка "ModuleNotFoundError: No module named 'app'"

Эта ошибка возникает, если:

1. **Вы запускаете команду не из корневой директории проекта**
   - ❌ Неправильно: запуск из папки `app/` или любой другой
   - ✅ Правильно: запуск из корня проекта (`/Users/ivanfrolovich/Desktop/flowmance`)

2. **Виртуальное окружение не активировано**
   - Убедитесь, что видите `(flow)` в начале строки терминала
   - Если нет, активируйте: `source flow/bin/activate` (macOS/Linux) или `flow\Scripts\activate` (Windows)

3. **Решение:**
   ```bash
   # 1. Перейдите в корень проекта
   cd /Users/ivanfrolovich/Desktop/flowmance
   
   # 2. Активируйте виртуальное окружение
   source flow/bin/activate  # macOS/Linux
   # или
   flow\Scripts\activate     # Windows
   
   # 3. Запустите приложение
   uvicorn app.main:app --reload
   ```

   Или просто используйте скрипт запуска: `./run.sh` (macOS/Linux) или `run.bat` (Windows)

## 6. Проблемы с базой данных

Если при запуске появляется ошибка подключения к БД - это нормально. Приложение запустится, но таблицы не будут созданы автоматически. Убедитесь, что:
- PostgreSQL запущен
- Пароль в `app/core/database.py` правильный
- База данных `notswift` существует

