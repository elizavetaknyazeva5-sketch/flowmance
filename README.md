# Flowmance

FastAPI приложение для управления проектами и агентами.

## 🚀 Быстрый старт

### Вариант 1: Использование скрипта запуска (рекомендуется)

**macOS/Linux:**
```bash
./run.sh
```

**Windows:**
```bash
run.bat
```

### Вариант 2: Ручной запуск

1. **Перейдите в корень проекта:**
   ```bash
   cd /Users/ivanfrolovich/Desktop/flowmance
   ```

2. **Активируйте виртуальное окружение:**
   ```bash
   # macOS/Linux
   source flow/bin/activate
   
   # Windows
   flow\Scripts\activate
   ```

3. **Установите зависимости (если еще не установлены):**
   ```bash
   pip install -r flow/requirements.txt
   ```

4. **Запустите приложение:**
   ```bash
   uvicorn app.main:app --reload
   ```

5. **Откройте в браузере:**
   - http://localhost:8000 - главная страница
   - http://localhost:8000/docs - документация API (Swagger)

## ⚠️ Частые ошибки

### "ModuleNotFoundError: No module named 'app'"

**Причина:** Команда запущена не из корневой директории проекта.

**Решение:**
1. Убедитесь, что вы находитесь в `/Users/ivanfrolovich/Desktop/flowmance`
2. Активируйте виртуальное окружение
3. Используйте скрипт `./run.sh` или `run.bat` для автоматического запуска

## 📚 Подробная документация

См. [INSTALL.md](INSTALL.md) для подробных инструкций по установке и настройке.

